import { productsOptions } from '../data/options';

export default {
  fields: ['products'],
  labels: {
    products: 'Products - Single Select (react-select)',
  },
  extra: {
    products: productsOptions,
  },
  values: {
    products: 'iMac',
  },
};
